{
  "private": true,
  "scripts": {
    "production": "cross-env NODE_ENV=production node_modules/webpack/bin/webpack.js --no-progress --hide-modules --config=node_modules/laravel-mix/setup/webpack.config.js && mkdir -p data && mv static/mix-manifest.json data/manifest.json"
  },
  "devDependencies": {
    "cross-env": "^5.1",
    "laravel-mix": "^2.0",
    "lodash": "^4.17.13",
    "poosh-cli": "^2.0.0",
    "poosh-plugin-s3": "^2.0.0"
  }
}
