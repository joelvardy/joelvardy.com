{% extends "layouts/default.twig" %}

{% block body %}

    <section page="{{ slug }}">

        <header class="half-page">

            <nav>
                <a href="{{ path_for('about') }}" title="About Joel Vardy">About</a>
                <a class="active" href="{{ path_for('projects') }}" title="Projects I've been involved in">Projects</a>
                <a href="{{ path_for('writing.list') }}" title="Joel's Ramblings">Writing</a>
            </nav>

            <hgroup>
                <h1><a href="{{ path_for('about') }}" title="About Joel Vardy">Joel Vardy</a></h1>
                <h4>Need a <span>Web Developer</span>? <a href="{{ path_for('about') }}#contact" title="Contact Joel Vardy">Get in touch</a>!</h4>
            </hgroup>

        </header>

        <section id="content">

            <h2>Projects</h2>

            <p>Below are some of the projects I have been involved in, if you would like a full CV please <a href="{{ path_for('about') }}#contact" title="Contact Joel Vardy">get in touch</a>.</p>

            {% for project in projects %}
                {% include project %}
            {% endfor %}

        </section>

    </section>

{% endblock %}
